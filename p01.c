# include <stdio.h>
// Sum of two numbers
int main()
{
    int sum;
    int a, b;
    printf( "Enter a and b:");
    scanf( "%d%d", &a, &b );

    sum = a + b;
    printf("%d", sum);
    return 0;
}
